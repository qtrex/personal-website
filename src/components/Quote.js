import React from "react";

function Quote() {
    return (
        /* Returns the quote section of webpage */
        <div className="container-fluid center-text">
            <p className="quote-margin quote-content">If debugging is the process of removing software bugs, then programming must be the process of putting them in.</p>
            <p className="quote-author">-Edsger W. Dijkstra</p>
        </div>
    );
}

export default Quote;